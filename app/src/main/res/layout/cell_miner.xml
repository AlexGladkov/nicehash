<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/flMiner"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    android:background="?attr/colorBackgroundPrimary" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal">

        <ImageView
            android:layout_width="@dimen/defaultBigIconSize"
            android:layout_height="@dimen/defaultBigIconSize"
            android:layout_gravity="center_vertical"
            android:src="@drawable/ic_icons_nvidia"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_gravity="center_vertical">

            <TextView
                android:id="@+id/txtMinerTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:fontFamily="sans-serif-medium"
                android:textColor="?attr/textColorPrimary" />

            <TextView
                android:id="@+id/txtMinerHash"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="11sp"
                android:textColor="@color/text_color_hint"
                tools:ignore="SmallSp" />

        </LinearLayout>

        <ImageView
            android:id="@+id/btnMinerMore"
            android:layout_width="@dimen/defaultIconSize"
            android:layout_height="@dimen/defaultIconSize"
            android:layout_gravity="center_vertical"
            android:src="@drawable/ic_miner_more"
            android:layout_marginEnd="16dp" />

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/default_separator_size"
        android:background="?attr/dividerColor"
        android:alpha="0.2"
        android:layout_gravity="bottom" />

</FrameLayout>